# LifeOS · 常驻桌面悬浮窗（Widget）视觉规格

> 橙白 v4 同源 · 玻璃拟态 · 320×140 · frameless + alwaysOnTop · 右下角常驻
> 设计原则沿用 v4：**留白是奢华，橙色是呼吸**；任一态橙色 ≤ 3 处；小字号一律深色档。

---

## 一、关键设计说明（3 条）

### 1. 玻璃三层结构：毛玻璃 + 晨光 + 发丝描边
桌面悬浮窗没有 app 内暖白底色托底，所以玻璃要自己"够亮"：
- **底**：暖白渐变 `rgba(255,255,255,.74) → rgba(250,247,242,.52)`，保证深色壁纸上黑字仍然 ≥4.5:1 可读；
- **雾**：`backdrop-filter: blur(26px) saturate(1.6)`，透过桌面背景但保持字形锐利（saturate 让壁纸颜色透进来时偏暖，与品牌一致）；
- **光**：`::before` 右上角暖橙 radial（坐标 88% -12%，正是 app 内 `body::before` 主光晕的坐标），让"晨光"从主界面延续到桌面；
- **边**：1px 白描边 + `inset 0 1px 0` 顶部高光 + 外层发丝暗线，模拟玻璃厚度；外阴影分两层——冷灰深影（悬浮）＋ 橙色环境光（暖），克制不霓虹。

### 2. 状态切换不重建玻璃：grid 叠放 + 交叉淡入
两种状态共用同一块玻璃面（blur、描边、光晕只画一次），内容层用 `grid-area: 1/1` 叠放，切换时仅 opacity + 7px 垂直漂移交叉淡入——无闪烁、无重排，对 alwaysOnTop 常驻窗口是性能友好的做法。轮播条目（每 3-4s）用**垂直漂移**进出（上滑离场/下滑入场），读感是"滚动提醒"而不是"翻页打扰"；计时态的"正在专注"氛围只靠两处动效：整片玻璃的 `focusBreath` 呼吸光 + 一颗脉动橙点，安静不喧宾夺主。

### 3. 一寸屏只给一件事：v4 信息规则原样移植
- 空闲态：eyebrow 深橙小字（10.5px / accent-deep，≥5:1）＋ 任务计数 `tabular-nums` 防跳动 ＋ 顶部 3px 微进度线；
- 计时态：40px 渐变大数字复用 app 内 `.timer-big` 的 `background-clip: text` 手法（按 140 高缩放），模式胶囊为唯一实底橙（渐变＋`--glow-accent`），任务名 ellipsis；
- 分类色点 `--dot` 取习惯色板（`--info/--success/...`），图标不用 emoji 保证玻璃面干净；
- `prefers-reduced-motion` 一键收口所有动画，与 v4 全局动效纪律一致。

---

## 二、HTML 骨架（widget.html，独立 BrowserWindow 加载）

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>LifeOS Widget</title>
<link rel="stylesheet" href="css/widget.css">
<!-- 若并入主应用：把 widget.css 追加到 style.css 末尾，并把 <body> 背景设为透明 -->
<style>
  html, body { background: transparent; margin: 0; overflow: hidden; }
</style>
</head>
<body>
<div class="widget" id="widget" data-state="idle" data-mode="pomodoro"
     role="region" aria-label="LifeOS 桌面悬浮窗">

  <!-- ══ 空闲态：今日计划轮播 ══ -->
  <div class="wid-states">
    <section class="wid-state wid-idle">
      <header class="wid-head">
        <span class="wid-eyebrow">今日计划</span>
        <span class="wid-count">任务 <b id="idleCount">2/5</b></span>
      </header>

      <div class="wid-carousel" id="carousel" aria-live="off">
        <!-- 每 3-4s：旧条目加 .leave，新条目加 .on + .enter（见文末 JS 伪码） -->
        <article class="wid-item on enter">
          <div class="wid-item-title">
            <i class="wid-item-dot" style="--dot: var(--info)"></i>
            <span>线性代数 · 第四章习题</span>
          </div>
          <div class="wid-item-meta">课程 · 10:00 · 教三 205</div>
        </article>
        <article class="wid-item">
          <div class="wid-item-title">
            <i class="wid-item-dot" style="--dot: var(--success)"></i>
            <span>写产品周报</span>
          </div>
          <div class="wid-item-meta">任务 · 截止 18:00</div>
        </article>
        <article class="wid-item">
          <div class="wid-item-title">
            <i class="wid-item-dot" style="--dot: var(--accent)"></i>
            <span>英语听力 30min</span>
          </div>
          <div class="wid-item-meta">习惯 · 已连续 12 天</div>
        </article>
      </div>

      <div class="wid-dots" id="widDots">
        <i class="wid-dot on"></i><i class="wid-dot"></i><i class="wid-dot"></i>
      </div>
    </section>

    <!-- ══ 计时态：大数字 + 模式 + 专注氛围 ══ -->
    <section class="wid-state wid-timer">
      <header class="wid-mode">
        <span class="wid-mode-pill" id="modeLabel">番茄</span>
        <span class="wid-task" id="taskName">写 LifeOS 悬浮窗规格</span>
        <i class="wid-live-dot" title="正在专注"></i>
      </header>

      <div class="wid-time" id="timerTime">25:00</div>

      <div class="wid-timer-bar" aria-hidden="true">
        <i id="timerFill" style="width:64%"></i>
      </div>
    </section>
  </div>

  <!-- 空闲态专属顶部微进度 -->
  <div class="wid-progress" aria-hidden="true"><i id="idleProgress" style="width:40%"></i></div>
</div>

<script>
  /* 状态切换：widget.dataset.state = 'timer' | 'idle'（玻璃不重建，仅内容交叉淡入）
     timerFill.style.width = 剩余比例%  （data-mode="countup" 时进度条自动隐藏）
     轮播：每 3-4s 把当前条目加 .leave，下一条目加 .on + .enter；同步 widDots 的 .on */
</script>
</body>
</html>
```

### Electron 主进程创建参数（落地清单）

```js
const { BrowserWindow, screen } = require('electron');
const { workArea } = screen.getPrimaryDisplay();
const wid = new BrowserWindow({
  width: 320, height: 140,
  frame: false,            // frameless
  transparent: true,       // 允许毛玻璃透出桌面
  alwaysOnTop: true,       // 置顶
  resizable: false,
  skipTaskbar: true,       // 不占任务栏
  hasShadow: true,         // 由系统投影（与 CSS 阴影叠加）
  show: false,
  webPreferences: { contextIsolation: true }
});
wid.loadFile('frontend/widget.html');
wid.setPosition(workArea.x + workArea.width - 320 - 16,
                workArea.y + workArea.height - 140 - 16); // 右下角 16px 安全边距
wid.show();
```

---

## 三、完整 CSS（widget.css，可直接粘贴，约 82 行）

```css
/* ============================================================
   LifeOS · 桌面悬浮窗 Widget（橙白 v4 同源 · 玻璃拟态）
   320×140 · 右下角常驻 · frameless + alwaysOnTop · 双态同窗切换
   原则：留白是奢华，橙色只亮一处；小字号一律深色档（同 v4 规则）
   依赖：style.css 的 :root 令牌（--accent-grad / --radius-lg /
         --shadow-3 / --glow-accent / --border-strong 等）
   ============================================================ */
.widget {
  position: relative;
  width: 320px;
  height: 140px;
  border-radius: var(--radius-lg);
  background: linear-gradient(165deg, rgba(255,255,255,.74), rgba(250,247,242,.52));
  border: 1px solid rgba(255,255,255,.6);
  box-shadow:
    0 0 0 1px rgba(38,34,29,.06),            /* 外层发丝暗线 */
    inset 0 1px 0 rgba(255,255,255,.75),     /* 玻璃顶高光 */
    inset 0 -1px 0 rgba(38,34,29,.04),
    var(--shadow-3),
    0 12px 32px rgba(234,88,12,.14);         /* 橙色环境光 */
  backdrop-filter: blur(26px) saturate(1.6);
  -webkit-backdrop-filter: blur(26px) saturate(1.6);
  overflow: hidden;
  color: var(--text);
  font-family: var(--font);
  user-select: none;
  -webkit-app-region: drag;                  /* 整窗可拖拽 */
}
/* 晨光层：右上暖橙透光 + 左下淡琥珀（坐标对齐 app 内 body::before 主光晕） */
.widget::before {
  content: ""; position: absolute; inset: 0; pointer-events: none;
  border-radius: inherit;
  background:
    radial-gradient(120% 92% at 88% -12%, rgba(251,146,60,.28), rgba(249,115,22,.06) 52%, transparent 72%),
    radial-gradient(90% 70% at -8% 112%, rgba(253,186,116,.16), transparent 60%);
}
/* 计时态专注氛围：整片玻璃的呼吸暖光 */
.widget[data-state="timer"]::after {
  content: ""; position: absolute; inset: 0; pointer-events: none;
  border-radius: inherit;
  background: radial-gradient(120% 90% at 50% 0%, rgba(249,115,22,.15), transparent 60%);
  animation: focusBreath 3.6s ease-in-out infinite;
}
/* 空闲态顶部微进度（3px 发丝） */
.wid-progress { position: absolute; top: 0; left: 0; right: 0; height: 3px; z-index: 2; background: rgba(38,34,29,.05); }
.wid-progress i { display: block; height: 100%; background: var(--accent-grad); border-radius: 0 3px 3px 0; transition: width .6s ease; }
.widget[data-state="timer"] .wid-progress { display: none; }
/* 内容层：grid 叠放，双态交叉淡入，玻璃只画一次 */
.wid-states { position: relative; z-index: 1; display: grid; height: 100%; padding: 15px 16px 12px; }
.wid-state { grid-area: 1 / 1; display: flex; flex-direction: column;
             transition: opacity .3s ease, transform .3s ease, visibility .3s; }
.wid-timer { opacity: 0; transform: translateY(7px); pointer-events: none; visibility: hidden; }
.widget[data-state="timer"] .wid-idle { opacity: 0; transform: translateY(-7px); pointer-events: none; visibility: hidden; }
.widget[data-state="timer"] .wid-timer { opacity: 1; transform: none; pointer-events: auto; visibility: visible; }
/* —— 空闲态：今日计划轮播 —— */
.wid-head { display: flex; align-items: center; justify-content: space-between; }
.wid-eyebrow { font-size: 10.5px; font-weight: 800; letter-spacing: .2em; color: var(--accent-deep); text-transform: uppercase; }
.wid-count { font-size: 11px; font-weight: 700; color: var(--muted-2); font-variant-numeric: tabular-nums; }
.wid-count b { color: var(--text); font-weight: 800; }
.wid-carousel { position: relative; flex: 1; min-height: 0; margin-top: 7px; }
.wid-item { position: absolute; inset: 0; display: flex; flex-direction: column; justify-content: center;
            opacity: 0; pointer-events: none; }
.wid-item.on { opacity: 1; }
.wid-item.enter { animation: itemIn .5s cubic-bezier(.2,.9,.3,1.1) both; }
.wid-item.leave { animation: itemOut .42s ease both; }
.wid-item-title { font-size: 15px; font-weight: 700; letter-spacing: .01em;
                  display: flex; align-items: center; gap: 8px; white-space: nowrap; overflow: hidden; }
.wid-item-title span { overflow: hidden; text-overflow: ellipsis; }
.wid-item-dot { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0;
                background: var(--dot, var(--accent-grad)); box-shadow: 0 0 5px rgba(249,115,22,.45); }
.wid-item-meta { font-size: 12px; color: var(--muted-2); margin-top: 3px;
                 white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.wid-dots { display: flex; align-items: center; gap: 5px; margin-top: 9px; }
.wid-dot { width: 4px; height: 4px; border-radius: var(--radius-pill); background: var(--border-strong); transition: all .35s ease; }
.wid-dot.on { width: 15px; background: var(--accent-grad); }
/* —— 计时态：大数字 + 模式 + 专注氛围 —— */
.wid-mode { display: flex; align-items: center; gap: 8px; }
.wid-mode-pill { flex-shrink: 0; font-size: 10.5px; font-weight: 800; letter-spacing: .08em; color: #fff;
                 background: var(--accent-grad); padding: 2.5px 10px; border-radius: var(--radius-pill);
                 box-shadow: var(--glow-accent); }
.wid-task { flex: 1; min-width: 0; font-size: 12px; color: var(--muted-2);
            white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.wid-live-dot { width: 6px; height: 6px; border-radius: 50%; flex-shrink: 0;
                background: var(--accent-grad); animation: livePulse 2.4s ease-in-out infinite; }
.wid-time { margin-top: 7px; font-size: 40px; font-weight: 800; letter-spacing: -.02em; line-height: 1.04;
            font-variant-numeric: tabular-nums;
            background: var(--accent-grad);
            -webkit-background-clip: text; background-clip: text;
            -webkit-text-fill-color: transparent; }
.wid-timer-bar { margin-top: 9px; height: 3px; border-radius: var(--radius-pill);
                 background: rgba(38,34,29,.07); overflow: hidden; }
.wid-timer-bar i { display: block; height: 100%; border-radius: inherit;
                   background: var(--accent-grad); transition: width .4s ease; }
/* 正计时无剩余比例，隐藏进度条 */
.widget[data-state="timer"][data-mode="countup"] .wid-timer-bar { visibility: hidden; }
/* —— 动效 —— */
@keyframes itemIn      { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: none; } }
@keyframes itemOut     { from { opacity: 1; transform: none; } to { opacity: 0; transform: translateY(-11px); } }
@keyframes focusBreath { 0%, 100% { opacity: .5; } 50% { opacity: 1; } }
@keyframes livePulse   { 0%, 100% { opacity: 1; transform: scale(1); } 50% { opacity: .5; transform: scale(.8); } }
/* 尊重系统"减少动态效果"（同 v4 全局收口） */
@media (prefers-reduced-motion: reduce) {
  .widget *, .widget::before, .widget::after { animation: none !important; }
}
```

---

## 四、验收要点

- 深色壁纸上黑字仍清晰（玻璃底 ≥52% 暖白不透明度）；浅色壁纸上玻璃自然"隐形"，靠发丝描边 + 双阴影保持边界；
- 空闲态 3-4s 轮播只动内容层，玻璃无闪烁；切换计时态仅 0.3s 交叉淡入；
- 每态橙色使用处数：空闲 3（eyebrow / 进度线 / 色点），计时 3（模式胶囊 / 大数字 / 脉动点）——符合 v4"一屏橙 ≤4 类"；
- `prefers-reduced-motion` 下所有动效静默。
